# esjzone-blocker

用于在 ESJ Zone 小说列表中批量选择并隐藏不想看到的作品。

页面右下角的“屏蔽”按钮用于展开操作选项。进入选择模式后，点击小说卡片任意位置即可切换选择状态。

选择面板支持本页全选、清空选择和已选计数；同一作品在页面出现多次时会同步处理。浮动入口仅在检测到小说卡片的页面显示。

## 安装

请确保浏览器已经安装 Tampermonkey，然后打开本项目的 Releases 页面，在对应版本的 Assets 区域点击 `esjzone-blocker.user.js`。Tampermonkey 会打开脚本安装确认页面。
